# FRESH-SESSION HANDOFF — coordinator (2026-07-13)

You are S0, the coordinator. Full board + gap map = `status/S0-coordinator.md` (read it). This is the fast orient.

## State (clean)
- **HEAD `7723285`** on `build/persona-v5`. **Suite 306 passing, tree clean** (no uncommitted product code).
- **Security H1/M1 deployed + verified** — server on :8137 runs the fixed code (`run_shell cwd=self` refused). `qtest` $5 test proved the system works end-to-end on a fresh topic (81 claims / $5).
- The 6 terminals: **idea + review live & productive; imp1 + imp2 committed their lanes** (Lane-4 legibility, Lane-1 teams — see git log). **imp3 (Membrane) + imp4 (Engine) terminals are DARK** — coordinator **worktree-workflow batches** build their lanes instead.

## In flight
- **Batch-2 workflow `w41yea9k5`** (worktree-isolated): kg invariants (F2.6 no-inflation guard, F2.7 validity window), F2.4 feasibility gate, F3.3 citation-vs-support divergence, F3.11 fieldmap→handoff + note-diff, A8 provenance section + wiring paper_lint into the compile path. **On completion: integrate worktrees → run full `pytest -q` → if green, commit lane-scoped (never `-A`).** (Batch-1 pattern: copy `.claude/worktrees/wf_*/persona/...` files into the main tree, verify, commit.)

## Remaining dark-lane backlog (gap map §"DARK lanes")
- **Lane-2 tail:** F2.8 coherence.drift→event/flag, F2.9 belief staleness/revisit (poisoning_signals still 0 consumers), F2.10 claim-level vector retrieval, F2.3 real conformal thresholds.
- **Lane-3 tail:** full F3.1 (PageRank load_bearing)/F3.2 (full VoI) beyond M0, F3.10 field-scoped failover. (F3.4/3.5/3.7/3.8/3.9 landed in batch-1.)
- **FC-8 oracle renderer** (imp1, against `ideas/I1.4a` envelope matrix); **I1.1 derivation core** (SynthesisStatement/InferenceStep + unsupported-synthesis gate).
- **RQ-E15** ($0-runnable now: synthetic policies + eval fixtures) to validate abstention magnitudes. **RQ-E16/E06/E17 + RQ-E02** need **human expert labels** (human-as-resolver) before their PLACEHOLDER metrics gate autonomy.

## How to run it
Coordinator = judge/planner. Dark lanes → worktree-isolated `Workflow` batches (disjoint files, tests each, $0). **Every coordinator-workflow's code goes through the review gate** (file `requests/S0--to--review--verify-*`); it caught 2 real bugs this session (PQ-REG-1, L-DEP-1). Commit only on human OK, lane-scoped, never `-A`. Budget: default $0; any paid run needs an explicit cap (`PERSONA_DAILY_BUDGET_USD`).
