# S2 audit — S4 (Lane 1) F1.1 branching/DAG investigations

**When:** 2026-07-13 · monitor fired on S4 ✅-count 3→4. **Verdict: PASS.** $0, suite 127.

## What
`research/investigation.py`: `_branch_plan(sub_hypotheses)` fans ≥2 independent sub-hypotheses into one `investigate` branch each (sharing `harvest`), joined by a single `consolidate`/synthesize step (DAG join via existing `queue.lease()` deps). `<2` subs → `DEFAULT_PLAN`. `create(..., sub_hypotheses=None)` kw-only, backward-compatible.

## Adversarial verification (S2 ran)
- `pytest tests/test_investigation_branch.py` → **4 passed**; full `pytest -q` → **127 passed** (floor ≥46).
- **DAG join is real, not mocked** (`test_branch_dag_join`): builds an actual `TaskQueue`, `launch()`es, leases in a loop, asserts `consolidate` leases **only after both `investigate` branches are terminal**. This is the load-bearing correctness property (join must not fire on incomplete branches) and it's tested against the real queue.
- **Backward-compat byte-identical** (`test_default_plan_byte_identical`): linear question → no `deps`/`q` metadata leaks, step types == `DEFAULT_PLAN`, and `plan.md` renders **byte-for-byte** as the legacy format. Existing linear investigations are provably unchanged.
- `<2` sub-hyp → linear fallback (`test_single_sub_hypothesis_falls_back_to_linear`). Correct: no DAG complexity unless a question needs branching (sound YAGNI call, stated in the code comment).
- **§10 cross-check:** structural feature, $0, no model calls, no deployment/superiority claim. ✓

## For S0
- Committable (lane-scoped): `research/investigation.py` (+ `daemon/*` already staged) + `tests/test_investigation_branch.py`.
- S4 track record this session: T4.1/T4.2, RQ-E06/E07c, Lane-1 M0/FC-1, F1.1 — **5/5 PASS**, all with real (non-mock) oracles and $0.
