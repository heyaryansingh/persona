# S2 review — commits 56956e1 (Review surface) + 7723285 (engine), 2026-07-13

Adversarial multi-agent review (6 surfaces × find→refute). **7 findings: 2 FIXED, 4 CONFIRMED-open, 1 REFUTED.**
Every finding was handed to an independent skeptic instructed to construct a real triggering input or refute.

## FIXED this review (committed, lane-scoped, tested)

- **`f6e49f6` — forensics.py:132 GRIMMER tautology (HIGH, fabricated-verdict).** `consistent = abs(ss-round(ss)) < 0.5`
  is a no-op (distance to nearest integer is always ≤0.5) → GRIMMER returned `ok` for **every** input and could never
  flag an impossible SD — the one thing the module exists to do. Replaced with the real Anaya-2016 rounding-band
  integer test. Verified: sd=0.50/mean=1.00/n=2 → inconsistent; sd=0.71/mean=1.50/n=2 → ok; sd=0 → ok. +2 tests.
- **`9cd5f98` — app.py:451 / audit.reaudit HTTP 500 (MED, correctness-crash).** 7723285 dropped `reaudit`'s `force`
  param; the on-demand `/reaudit` endpoint still calls `reaudit(force=True)` → TypeError → **500 on every on-demand
  reaudit**. Restored `force`, threaded to `watchlist.due(min_age_hours=0)` — also fixes the quieter regression that,
  even force-deleted, the endpoint would have honored the 12h staleness floor it is documented to ignore (18b8e07).
  Supervisor path keeps force=False → M2 busy-loop guard intact. +1 test.

## REFUTED (false positive — no action)

- **trajectory.py:36 (claimed HIGH, boundary-validation).** Claimed `_order()`'s bare `float(conf)` lets an
  unclamped/NaN confidence into the trajectory series. **Refuted:** `_order` reads confidence only from `Claim`
  nodes, and every write to `Claim.confidence` already routes through `_clean_confidence` (`add_claim` → kg.py:147,
  recompute averages cleaned `r.conf`, `anchor` hardcodes 0.99/0.01). No unclamped value can reach it. The upstream
  write-boundary clamp (S2 review-kg-hygiene, now incl. dep-edges) is exactly what makes this read-side safe.

## CONFIRMED-open — routed to owning lanes (S5/S6 parked → via S0)

- **darklit.py:66 (MED, silent-wrong-belief) → Lane 3 (+Lane 2 for the kg half).** `dead_science` derives
  `dormant_days` from `ingest_time`, which the docstring says is "refreshed every time a source re-observes the
  claim" — but kg.py writes `ingest_time` ONLY in `ON CREATE SET` (kg.py:157), no `ON MATCH` refresh, no other writer.
  So it is frozen at claim creation: an actively re-observed, high-support claim (support 6, last seen yesterday) is
  flagged as ~558-day "dead science". Doc-vs-behavior mismatch emitting an authoritative-but-wrong `dormant_days`.
  **Fix options:** (a) add `ON MATCH SET c.last_observed=$now` to `add_claim` and read that (Lane 2 write-semantics
  decision — additive field, safe), OR (b) correct darklit's docstring + derive dormancy from the newest
  `SUPPORTED_BY` edge time. (a) is the honest fix. MED because dead_science is read-only + self-labels PLACEHOLDER.
- **forensics.py:216 (MED/LOW, gate-bypassed) → Lane 3.** `min_detectable_effect`'s `groups != 2` not_applicable
  gate is unreachable: the only caller never passes group count, so a 3+-arm design silently gets a two-sample MDE
  verdict instead of `not_applicable`. **Fix:** thread the extracted group count from the caller to
  `min_detectable_effect(..., groups=k)` so the existing gate fires.
- **science.py:257 (MED, fail-loud-boundary) → Lane 3.** `call()` dispatch for `geo_lookup` hardcodes `ok:True`
  even for an invalid/uncached GEO id, contradicting the file's ok-means-data-found convention and PRD-03's fail-loud
  contract (`geo_lookup returns {ok:False, error} on an unresolvable id`). **Fix:** return `ok:False` + error on
  unresolved id; never present a silent empty dataset as found.
- **index.html:2074 (LOW, correctness) → Lane 4/S3.** "Sparse-evidence candidates" filter tests entity names
  (`b.subject/b.object`) against the `contested` Set, which holds only claim_ids (built at :2063 from
  `c.pos_claim/c.neg_claim`). A claim_id can never equal a gene name → the guard is dead for every input, so a
  conflict-linked one-source belief still shows in the panel that claims "their absence of conflict...". **Fix:**
  populate a separate Set from the conflicts' subject/object entity names (both keys exist on the payload, kg.py:270).

## Method note
Adversarial verify earned its keep: it **refuted** the trajectory HIGH (would have been a wasted lane dispatch) and
**corrected** the GRIMMER finding's own example (the proposed fix direction was right, the "impossible SD" example
it named was actually consistent — the fix landed on the real defect, not the mis-stated example).
