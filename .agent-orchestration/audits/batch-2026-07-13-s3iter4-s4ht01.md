# S2 audit batch — S3 Iteration-4 (mount+routes) + S4 RQ-HT01 scaffold

**When:** 2026-07-13, high-velocity burst. **Verdict: both PASS.** Suite 147, $0.

## S3 (Lane 4) Iteration-4 — `/static` mount + 5 epistemic routes — PASS
- **`/static` mount (P0.1):** `app.py` L20/L30 correct; distinct prefix, never shadows `/api` or `/`; root `/` (L1388) still serves `index.html`. Suite 144→147.
- **5 routes** (`/api/persona/{pid}/`): `eval` (FC-7 real → `run_oracle`), `engine/dependency`, `engine/value_queue`, `engine/handoff` (FC-2), `gate_decisions` (read-only ledger).
- **Verified non-mutating + graceful:** each returns `available:false`/`ok:false` when its parked backend (FC-4/FC-2) is absent; **no `.add`/`.commit`/membrane/KG write** in any route body (read app.py 1392-1457). This is the anti-invented-activity + membrane discipline — routes don't fabricate data or mutate beliefs. ✓
- `tests/test_epistemic_api.py` → 3 passed (mount + 5 routes registered + FC-7 provider contract).

## S4 (Lane 1) RQ-HT01 branch-vs-linear scaffold — PASS
- `experiments/exp_ht01_branch_vs_linear.py`: measures branch/DAG (F1.1) vs linear; **step counts are the real `investigation.py` code, not invented**; ≥20 bootstrap seeds (`MIN_SEEDS` enforced), mean±95% CI, F1.1 go/no-go gate.
- **Live arm HARD-gated:** `require_live()` → `PaidStageGated` unless `PERSONA_HT01_LIVE=1` + API-key probe; live path `NotImplementedError` until S0 greenlight. $0 default.
- `tests/test_engine_ht01_scaffold.py` → 5 passed.
- **§10 cross-check:** no deployment/superiority claim, $0, paid-gated, uses real code not fabrication. ✓

## Session scorecard (all PASS, all real/non-mock oracles, all $0)
S4: T4.1/T4.2 · RQ-E06/E07c · Lane-1 M0/FC-1 · F1.1 · RQ-HT01 = **6 milestones**.
S3: H1/M1 security (verified, not-live) · FC-7 M0 · Flagship 4.1 · Iteration-4 mount+routes = **4 milestones**.
No regressions; floor 46 → suite 147.

## Standing blockers (unchanged, not S2's to clear)
- **S5 (Lane 2) + S6 (Lane 3) PARKED** — need human restart. Blocks FC-2/3/5 real wiring, multi-rater/§A, M2 `watchlist` floor. S3/S4 correctly building against fixtures meanwhile.
- **Nothing committed** (HEAD `0a6f923`) — all verified work awaits human-authorized scoped commits (H1/M1 security is the priority; still live-vuln until commit+restart).
- **P0.2 index.html split DEPRIORITIZED** by S3/S0 — my P0.3 deferred (before-baseline preserved as insurance).
