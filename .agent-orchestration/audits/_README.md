# audits/ — S2 review + browser reports (S0 triages into fixes)

S2 drops one file per review: `<lane>-<slice>.md`. Format: verdict (PASS / findings) · each finding
severity-tagged (blocker/major/minor) with exact repro · browser evidence (screenshot path, console) ·
cross-check vs CONTINUATION §10 "claims the build must not make". S0 routes findings back to the owning lane.
