# S2 audit — S4 `✅` (T4.1 investigate finalizer + T4.2 RQ-E05 scaffold)

**When:** 2026-07-12 · triggered by S4 posting `✅ READY`.
**Verdict: PASS.** No blocking findings. Safe to commit (lane-scoped) when the human authorizes.

## Standing suite (S2 ran it — not trusting self-report)
| Check | Result |
|---|---|
| `python -m pytest -q` (FULL) | **57 passed** / 26.5s — up from 46 baseline (+11); **≥46 floor held** |
| `python -m compileall -q persona experiments` | exit 0 |
| `git diff --check` | clean except one benign `LF→CRLF` warning on `tests/ui_research_smoke.cjs` (S2's own file, not S4's) |
| `verify_conflict_reviews` | ok, 0 records (S4 didn't touch it) |

## T4.1 — investigate() failure finalizer — adversarially verified REAL
- **Code (root-cause seam):** `investigate()` creates `session`, then wraps the **entire** body — extracted into `_run_investigation(...)` (analyst.py L206+, all FS writes + the model/tool loop) — in `try: return _run_investigation(...) except Exception: _finalize_crashed(session, exc); return {ok:False, reason:crashed}` (L197–203). The only code before the `try` is the 3 early-return gates + `session` creation, so no path can leave a session `running`. This is a full-body guard, **not** a prefix patch.
- `_finalize_crashed` (L159) is itself fail-safe: `session.finalize("invalidated", ...)` in try/except so a finalizer failure (e.g. the same disk-full that crashed the run) can't mask the original crash.
- **Test is a genuine oracle** (`tests/test_engine_investigate_finalizer.py`): injects the *real* unguarded path — monkeypatches `anthropic.Anthropic` → `_BoomAnthropic.messages.create` raises `RuntimeError` — **and stubs all 3 entry gates** so execution actually reaches the crash (without that, the test would false-pass by returning early). Asserts (1) no exception escapes + structured `crashed`, (2) session `invalidated` + **no other session left `running`** (strong global invariant), (3) `verify_session.ok` + `checks.status == invalidated`.
- Closes CONTINUATION §4 follow-up ("`analyst.investigate` needs a failure finalizer … regression test").

## T4.2 — RQ-E05 retrieval harness — SCAFFOLD-ONLY, compliant
- `$0` default; deterministic offline stages only (BM25 + citation + abstention + primariness + latency) over a **NON-SCIENTIFIC** hand-authored fixture (explicitly labeled, not gold).
- Paid stages (rerank / answer+cite / exact-span / grader) **HARD-gated**: `require_live()` → `PaidStageGated` unless `PERSONA_E05_LIVE=1` + `config.have_key()`, and even then live path is `NotImplementedError` until S0 prices `MODEL_WORKER`. Dry-run correctness = `gated`, `paid_calls: 0`.
- Stochastic aggregates = mean ± 95% CI over 200 seeds (≥20 enforced). Self-check caught 2 real wiring bugs (stopword-faked confidence; min-max norm made abstention never fire).
- **CONTINUATION §10 cross-check:** no forbidden claim — no deployment claim, no dense/hybrid superiority, correctness explicitly `gated`, gold never synthesized. ✓

## Notes for S0
- S4's committable set is clean: `persona/agents/analyst.py`, `tests/test_engine_investigate_finalizer.py`, `experiments/exp_rq_e05_retrieval.py`, `tests/test_engine_e05_scaffold.py`, `results/rq_e05_retrieval.{json,png}`. Lane-scoped `git add` only.
- T4.2 **live** execution correctly remains gated on human budget + a live model probe — do not greenlight from motion.
