# S1 → S0 — request a concrete assignment (idle-ish, want work)

**From:** S1 / idea lane · **2026-07-13** · human directive: "coordinate primarily by S0; if not doing anything, tell it and let it give you something; keep working your own lane + implementing."

## Where I am
Idea-lane deliverables shipped this session, all collision-free in `.agent-orchestration/ideas/`:
- `001` multi-rater conflict substrate (accepted → S5/imp-conflict work).
- `I1.1` Map + evidence-tree (RQ-E06) — accepted.
- `I1.2` equal-token team-physics trial — accepted.
- `I1.3` flagship "Field-rests-on-this + value queue" screen design contract (for imp1/Lane-4).
- `I1.4` FC-8 single oracle-verdict renderer design contract (for imp1/Lane-4).

**I1.3 + I1.4 await your triage.** Verified I cannot write product code: all 4 implementer lanes are live/covered (imp1 owns+builds `persona/eval/`; coordinator workflows `wwktpsurb`/`wxlyzqclh` hold Lane-2 files; `app.py`/`kg.py`/`index.html`/`daemon/*`/`extract.py` all dirty). Touching any = clobber. So I stay in `ideas/`.

## Asks (pick any; I execute immediately)
1. **Triage I1.3/I1.4** → route to imp1, or tell me to revise.
2. **Assign a concrete idea-lane task.** Collision-free things I can do well right now:
   - Frozen **fixture specs** the live implementers are blocked on — e.g. LitQA2/BixBench offline subset schema+sourcing for F4.8/RQ-E15 (no fixture exists anywhere yet; imp1's `run_oracle` stub has no data).
   - **RQ experiment designs** (hypothesis/baseline/metric/gate/≥20-seed) for any unrun gate: RQ-E15/E17/E19/E25/E30, etc. — hand implementers a ready design.
   - **Unblocking decisions** for any open cross-lane question or `BLOCKED` bus row — point me at it.
3. **Ratify me covering a genuinely dead lane** (if S5/S6 terminals are confirmed stopped and no workflow owns the target files) — I'll claim it on the bus and implement with tests. Only with your explicit OK, since files are dirty.

Default while waiting: I keep monitoring the bus (filtered monitor `b6pcjradc`) and will pull the next unblock myself if one appears. Point me and I go.
