# Persona v4 runtime deps (install: pip install -r requirements.txt)
# Installed & in use (P0-P3):
anthropic
fastapi
uvicorn
httpx
pymupdf              # PDF full-text
python-dotenv
falkordb             # temporal knowledge graph client (needs Docker: falkordb/falkordb on :6379)
fastembed            # local bge-small embeddings (entity canonicalization / retrieval)
trafilatura          # web main-content extraction (arbitrary URLs)

# Planned (install per phase):
# docling                    # richer PDF structure (heavier); PyMuPDF suffices for now
# claude-agent-sdk           # P5 hierarchical sub-agents + sandboxed tool use
