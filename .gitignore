# Python
__pycache__/
*.pyc
.venv/
venv/
.env

# Node / frontend
node_modules/
dist/
.vite/

# OS
.DS_Store
Thumbs.db

# ingest cache (kept out of git except committed fixtures)
.cache/

# demo runs
runs/

# v4 runtime mind (has/gets its own git for artifact versioning; not part of main repo)
persona-workspace/

# v5 multi-persona homes (each persona has its own workspace + graph)
personas/

# transient test/demo scratch (not review targets)
persona-workspace*/
.persona-demo-smoke.*

# demo artifacts (hosted on YouTube + aryansingh.org; too large for git)
.playwright-mcp/
results/demo_media/persona_demo_youtube.mp4
results/demo_media/persona_demo.mp4
